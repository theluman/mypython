动态引用
  sys.getrefcount() 计数引用数量
增加对象引用计数场景
  对象创建时:
  将对象添加进容器时，类似list.append()
  当对象被当作参数传递给函数时
  为对象创建另外变量名
减少引用计数
  引用此对象的某变量名被显示销毁，del x
  给引用此对象的某变量名被重新赋值
  从容器中移除对象时，类似list.pop()
  容器本身被销毁

if
while
for
